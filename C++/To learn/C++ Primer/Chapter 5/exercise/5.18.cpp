#include<bits/stdc++.h>
using namespace std;
int main()
{
   do { // 应该添加花括号
        int v1, v2;
        cout << "Please enter two numbers to sum:" ;
        if (cin >> v1 >> v2)
            cout << "Sum is: " << v1 + v2 << endl;
   }while (cin);
   return 0;
}